# Shared Conventions

The conventions shared across every language's examples — addressing (`<pattern>/<channel>`,
always explicit), the `KUBEMQ_AMQP_URL` environment variable, ANONYMOUS-by-default auth, the
8-section per-variant README template, credit/settlement defaults, and channel naming — live in the
repository-root conventions file:

➡️ **[../SHARED-CONVENTIONS.md](../SHARED-CONVENTIONS.md)**

See also:

- **[README.md](README.md)** — the cross-language examples index (13 variants × 6 languages, coverage matrix, run commands).
- **[../docs/README.md](../docs/README.md)** — concepts, guides, and reference documentation.
